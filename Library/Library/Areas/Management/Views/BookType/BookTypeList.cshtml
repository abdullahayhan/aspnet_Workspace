@model List<BookType>
<h3 class="text-center">Kitap Tipi Listeleme Sayfası</h3>
<a class="btn btn-primary" style="float:right" asp-area="Management" asp-controller="BookType" asp-action="Create">Yeni Ekle</a>
<table class="table table-striped">
    <thead>
        <tr>
            <th>#</th>
            <th>Name</th>
            <th>Aksiyonlar</th>
        </tr>
    </thead>
    <tbody>
        @for (int i = 0; i < Model.Count; i++)
        {
        <tr>
            <th>@(i+1)</th>
            <td>@Model[i].Name</td>
            <td>
                <a class="big-icon" asp-area="Management" asp-controller="BookType" asp-action="Edit" asp-route-id="@Model[i].ID"><i class="bi bi-pencil-square"></i></a>
                <a class="big-icon margin-icon" onclick="return confirmDel();" asp-area="Management" asp-controller="BookType" asp-action="HardDelete" asp-route-id="@Model[i].ID"><i class="bi bi-x-circle-fill"></i></a>
                <script language="javascript">function confirmDel() { var agree=confirm("Bu içeriği silmek istediğinizden emin misiniz?\nBu işlem geri alınamaz!"); if (agree) { return true ; } else { return false ;} }</script>
            </td>
        </tr>
        }
    </tbody>
</table>